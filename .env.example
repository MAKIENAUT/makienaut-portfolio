# VroomBroom database
# The Vercel Prisma Postgres integration supplies these values. Keep the
# production values in Vercel and copy only development credentials locally.
OW_MAIN_DB_DATABASE_URL="postgres://USER:PASSWORD@pooled.db.prisma.io:5432/postgres?sslmode=require"
OW_MAIN_DB_POSTGRES_URL=""
OW_MAIN_DB_PRISMA_DATABASE_URL=""

# Back-office authentication
# Run `npm run auth:hash` and paste the generated scrypt value here.
ORBW_BACKOFFICE_PASSWORD_HASH="scrypt:replace-with-generated-salt:replace-with-generated-hash"
# Use a random secret of at least 32 characters to sign back-office sessions.
ORBW_AUTH_SECRET="replace-with-a-random-secret-of-at-least-32-characters"
